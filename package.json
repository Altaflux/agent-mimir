{
    "name": "agent-mimir-pck",
    "scripts": {
        "build": "turbo run build",
        "start": "dotenv -- node scripts/config-setup.js && cd .temp_custom_deps && yarn install && cd .. && cross-env MIMIR_CFG_PATH=$INIT_CWD/.temp_custom_deps yarn run internalStart",
        "internalStart": "yarn run build && dotenv -- yarn workspace agent-mimir-cli run start",
        "dev": "turbo run dev",
        "lint": "turbo run lint",
        "publish": "bash scripts/release-branch.sh && turbo run build lint && yarn run release -w agent-mimir && echo '🔗 Open https://github.com/Altaflux/agent-mimir/compare/release?expand=1 and merge the release PR'",
        "format": "prettier --write \"**/*.{ts,tsx,md}\""
    },
    "author": "Altaflux",
    "repository": {
        "type": "git",
        "url": "https://github.com/Altaflux/agent-mimir.git"
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "dotenv-cli": "latest",
        "eslint": "^7.32.0",
        "eslint-config-custom": "*",
        "fs-extra": "^11.1.1",
        "prettier": "^2.5.1",
        "release-it": "^15.10.0",
        "turbo": "latest"
    },
    "packageManager": "yarn@3.5.1",
    "workspaces": [
        "agent-mimir",
        "agent-mimir-cli",
        "tools/*",
        ".temp_custom_deps"
    ]
}
