{
    "name": "agent-mimir-pck",
    "private": false,
    "scripts": {
        "build": "turbo run build",
        "start": " dotenv -- node scripts/custom-dependencies.js && npm install --prefix .temp_custom_deps && dotenv -- npm run internalStart --prefix .temp_custom_deps",
        "internalStart": "npm run build && dotenv -- npm run start -w agent-mimir-cli",
        "dev": "turbo run dev",
        "lint": "turbo run lint",
        "format": "prettier --write \"**/*.{ts,tsx,md}\""
    },
    "devDependencies": {
        "eslint": "^7.32.0",
        "eslint-config-custom": "*",
        "prettier": "^2.5.1",
        "turbo": "latest"
    },
    "packageManager": "npm@9.5.0",
    "workspaces": [
        "agent-mimir",
        "agent-mimir-cli"
    ]
}