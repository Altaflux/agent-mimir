{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "start": "dotenv -- npm run start -w agent-mimir-cli",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-custom": "*",
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "name": "agent-mimir-pck",
  "packageManager": "npm@9.5.0",
  "workspaces": [
    "agent-mimir",
    "agent-mimir-cli"
  ]
}
